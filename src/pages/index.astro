---
import TimeGrid from '../components/TimeGrid';
import BaseLayout from '../layouts/BaseLayout.astro';
import { TimeApp } from '../models/TimeApp';

const apps: TimeApp[] = [
	{ title: 'Time Between', link: '/between' }
]
---

<BaseLayout>
	<TimeGrid apps={apps} client:visible />
</BaseLayout>