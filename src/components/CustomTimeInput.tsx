import { TimeInput } from "@mantine/dates"
import { IconClock } from "@tabler/icons"
import { useRef } from "react"

export default function CustomTimeInput({ label, defaultValue }) {

    
    return (
        <>
            
        </>
    )
}